#!/usr/bin/env node
let MyApp = require('../app').default;
const conf = process.env.NODE_ENV === 'production' ? require("../config.json") : require("../config.dev.json");

if (process.env.HOT) {
  conf.env.hot = true;
}

let app = MyApp.getInstance(conf);
app.start(process.env.PORT || '3000')