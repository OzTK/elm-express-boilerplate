#!/usr/bin/env node

/**
 * Module dependencies.
 */

var testExpressApp = require('../app')
var testExpressServer = require('../server')

var app = new testExpressApp.default()
var srv = new testExpressServer.default(app)
srv.start(process.env.PORT || '3000')
